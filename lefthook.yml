pre-commit:
  parallel: true
  commands:
    fmt:
      run: cargo fmt --all -- --check
    clippy:
      run: cargo clippy -- -D warnings
    test:
      run: cargo nextest run

pre-push:
  commands:
    verify:
      run: pwsh ./verify.ps1
